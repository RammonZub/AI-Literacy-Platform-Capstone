/**
 * Shared constants for first-party anonymous visitor cookies.
 */

/** One-year max-age: stable anonymous linkage; still finite for hygiene. */
export const TRACKING_COOKIE_MAX_AGE_SECONDS = 60 * 60 * 24 * 365;
